---
const keywords = [
  'Public Policy Education',
  'Democracy & Human Rights',
  'Community Dialogue',
  'Critical Thinking',
  'Gotong Royong',
  'Aachen-Indonesia',
  'Civic Participation',
  'Independent Media',
  'Climate Action',
  'Social Justice',
]
---

<div class={`
  overflow-hidden border-b border-border bg-foreground text-background
`}>
  <div class="overflow-hidden py-2">
    <div class="inline-flex w-max animate-marquee">
      {
        Array.from({ length: 2 }).map(() => (
          <div class="flex">
            {keywords.map((keyword) => (
              <span
                class={`
                  font-mono text-[10px] tracking-[0.3em] whitespace-nowrap
                  uppercase
                `}
              >
                {keyword}
                <span class="inline-block px-8">•</span>
              </span>
            ))}
          </div>
        ))
      }
    </div>
  </div>
</div>
