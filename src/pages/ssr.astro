---
/**
 * This page is required to trick Astro into thinking that we use hybrid rendering.
 *
 * This is a workaround for a known bug where Astro doesn't properly detect hybrid rendering mode.
 * Bug details: https://github.com/withastro/astro/issues/12744
 */

export const prerender = false
---
